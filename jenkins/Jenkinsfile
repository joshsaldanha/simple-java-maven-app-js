pipeline {
    agent {
        docker { 
            image 'maven:3.9.6-eclipse-temurin-21'
            args '-v $HOME/.m2:/home/gse/.m2'
        }
    }
    stages {
        stage('Build') {
            steps {
                sh """
                   mvn -version
                   ls
                   mvn -Dmaven.repo.local=/home/gse/.m2/repository -B clean package
                """
            }
        }
    }
}
